description = "Define what is in and out of scope for the current effort and save as specs/SCOPE.md. Use when user wants a PRD, scope definition, or before plan-release on a new initiative.model: sonnet"
prompt = "@{commands/prompts/scope-work.md}"
